# Задачи (Memory Bank)

## Текущая задача
- **UI**: починить адаптив в **landscape (горизонтальная ориентация) на мобилках** — привести layout к читаемому виду, убрать наезды/обрезания.

## Сложность
- **Level 2 (Simple Enhancement)** — затрагивает responsive-стили/лейаут, вероятно несколько UI-компонентов.

## План
- Добавить детекцию **mobile landscape** и режим “fit-to-viewport” (масштабировать сцену как на десктопе, но меньше).
- Реализовать вычисление `scale` от размеров viewport (и safe-area на iOS) и применить `transform: scale(...)` к единому контейнеру UI так, чтобы **всё помещалось без скролла**.
- При необходимости в landscape ослабить пару жёстких `min-*`/отступов на базе `vh`, чтобы масштаб не становился слишком мелким.
- Прогнать руками на iOS Safari/Chrome: портрет/ландскейп, проверить кликабельность кнопок, отсутствие наездов, корректность “fixed” баннеров.

## Чеклист
- [x] Зафиксировать условия включения “landscape-fit” (touch + `(orientation: landscape)` + высота `<= 520px`).
- [x] В `src/ui/screens/balatro-inferno/BalatroInferno.jsx` добавить wrapper-контейнер сцены + расчёт `scale` от `window.innerHeight` (resize/orientationchange).
- [x] В `src/balatroInferno.css` добавить CSS-класс для масштабирования и `transform-origin`.
- [x] Точечно поправить landscape-минимумы (нижний `mb` под картами и `min-height` контролов) под `landscapeFitEnabled`.
- [x] Smoke-check: `npm run build` ок, тесты ок; desktop/portrait не затронуты (включение только по touch+landscape+низкой высоте).

---

## Последние завершённые
- (завершено) UI: логотип RESIMPLE GAMES — редкий auto-burst + burst на hover/tap (кулдаун ~800ms), CRT tear + RGB split (`archive-2025-12-23-logo-glitch-burst.md`)
- (завершено) UI: отключён normal-режим (скрыт из UI + контроллер форсирует CASCADE), шапка упрощена (убран MODE, кнопки PAYTABLE и /dev перенесены под плашки CHIPS/ANTE), добавлена стильная иконка 💵 (`archive-2025-12-23-disable-normal-mode.md`).
- (завершено) UI (mobile/узкие): на `<640px` скрыт “MAX WIN 150.000x” в шапке, шапка стала 2‑колоночной (CHIPS/ANTE), добавлены `truncate` и mobile‑порог для `formatMoneyAdaptive` (`archive-2025-12-23-mobile-header-maxwin-hide.md`).
- (завершено) UI: Paytable — добавлен блок “DECK CLEAR” (без слова jackpot) с условием полной очистки 54‑картной колоды за 1 каскад и выплатой **150.000x**; унифицирован формат **150.000x** в MAX WIN оверлеях (`archive-2025-12-23-deck-clear-paytable.md`).
- (завершено) UI: Paytable Modal — таблица выплат с примерами рук (миниатюры с подсветкой) и возможностью менять ставку (+/-) для просмотра потенциальных выигрышей. Триггер в верхней панели (`archive-2025-12-23-paytable.md`).
- (завершено) UI/CASCADE: история выигрышей каскада — `WINS (N)` + модалка (scroll) + mini-cards + блокировка во время каскада (`archive-2025-12-23-cascade-win-history.md`)
- (завершено) UI (CASCADE): running `WIN` под `CASCADE MULT` + анимация инкремента + показ на финале + без layout jump (`archive-2025-12-23-cascade-running-win.md`)
- (завершено) Баланс: Pair payout 0.3x → 0.2x (`archive-2025-12-23-pair-0.2x.md`)
- (завершено) UI/UX CASCADE: preview множителя (current/next) + **x1 всегда горит** (OFF убран).
- (завершено) UI: “MAX WIN 150.000x” — вернуть золотой стиль с переливом (починить/добавить `text-gold-shimmer`).
- (завершено) AutoPlay: модалка выбора автоспинов + авто-цикл + счётчик на PLAY + STOP на AUTO + pixel/CRT стиль + shake.
- (завершено) Dev Mode: DevTools — раздел “Добавить денег” (кнопки +1000/+5000/+10000/+100000/+1m/+10m) + безопасный handler + dev-log.
- (завершено) UI: плашка CHIPS — overflow больших сумм (adaptive формат + tooltip, без переносов).
- (завершено) UI: увеличить высоту блока нижних кнопок (TURBO/PLAY/AUTO/+/-) и затем уменьшить на ~10% по фидбэку.
- (завершено) UI: `MaxWinPoster` на full desktop — ограничить раздувание и косметика “150.000x” (x меньше, больше воздуха).
