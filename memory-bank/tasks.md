# Задачи (Memory Bank)

## Текущая задача
Адаптивная верстка: поднять карты (HandBoard) и блок cascade mult (CascadeMultiplierIndicator) ближе к центру на разрешениях 407x958, 510x958, 702x958, 830x958

## Контекст/цели
- Что хотим получить: на указанных разрешениях карты и индикатор каскадного множителя должны быть ближе к центру экрана
- Ограничения/инварианты: не ломать существующую адаптивность на других разрешениях, сохранить визуальный стиль
- Риски: возможны конфликты с существующими media queries

## План (микро‑шаги)

### Этап 1: Анализ текущей структуры
- [x] Проанализировать текущую структуру layout компонентов HandBoard и CascadeMultiplierIndicator
  - Контейнер с картами находится в строке 1074: `<div className="flex flex-col items-center gap-[clamp(10px,2vh,24px)] w-full mt-auto mb-[clamp(52px,7.5vh,130px)]">`
  - `mt-auto` прижимает контейнер к низу родительского flex-контейнера
  - Родительский контейнер (строка 1018) использует `flex-1` и `justify-center`

### Этап 2: Создание адаптивных стилей
- [x] Добавить кастомный CSS класс в `src/balatroInferno.css` для указанных разрешений
  - Создать класс `.hand-board-container-adaptive` с медиа-запросами
  - Медиа-запрос: `@media (min-width: 400px) and (max-width: 840px) and (min-height: 950px) and (max-height: 970px)`
  - Заменить `margin-top: auto` на конкретное значение (например, `margin-top: clamp(20px, 5vh, 40px)`)
  - Возможно, скорректировать нижний отступ для баланса

### Этап 3: Применение стилей
- [x] Применить новый класс к контейнеру в `BalatroInferno.jsx` (строка 1074)
  - Заменить `mt-auto` на условный класс или добавить класс к существующим
  - Убедиться, что стили не конфликтуют с существующими breakpoints

### Этап 4: Тестирование
- [ ] Визуальная проверка на разрешениях: 407x958, 510x958, 702x958, 830x958
- [ ] Проверить, что на других разрешениях ничего не сломалось
- [ ] Убедиться, что элементы действительно ближе к центру на указанных разрешениях

## Гейты/проверки
- [ ] `npm test`
- [x] `npm run build` — успешно (931ms)
- [x] `npm run lint` — ошибок нет
- [ ] Визуальная проверка на разрешениях: 407x958, 510x958, 702x958, 830x958

## Заметки
- **Файлы для изменения:**
  - `src/balatroInferno.css` - добавить кастомный класс с медиа-запросами
  - `src/ui/screens/balatro-inferno/BalatroInferno.jsx` (строка 1074) - применить новый класс к контейнеру
- **Подход:** использовать кастомный CSS с медиа-запросами вместо Tailwind arbitrary variants (более надежно для специфичных разрешений)
- **Медиа-запрос:** диапазон для покрытия всех указанных разрешений: `(min-width: 400px) and (max-width: 840px) and (min-height: 950px) and (max-height: 970px)`
- **Инварианты:** не ломать существующую адаптивность на других разрешениях, сохранить визуальный стиль
