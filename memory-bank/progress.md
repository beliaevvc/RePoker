# Прогресс (Memory Bank)

## Текущий статус
- 🚧 В работе: **починка mobile landscape адаптива** на основном экране — добавляем “fit-to-viewport scale” для touch-ландскейпа, не трогая desktop/portrait.

## Что сделано
- [x] Детектирован проблемный кейс: `h-[100svh]` + `overflow-hidden` и жёсткие `vh/min-*` приводят к наездам при маленькой высоте в landscape.
- [x] Добавлен **landscape-fit scale** (только touch + landscape + `innerHeight <= 520`) на контейнер сцены в `src/ui/screens/balatro-inferno/BalatroInferno.jsx`.
- [x] Исправлена эвристика scale: вместо `scrollHeight` (не отражал наезды из-за flex/fixed) — масштаб считается от `window.innerHeight` (target ~460px, clamp до `>= 0.78`).
- [x] В landscape дополнительно ужаты два проблемных минимума под `landscapeFitEnabled`: нижний `margin-bottom` у блока с картами и `min-height` у сетки контролов.
- [x] Добавлен CSS-хелпер `.repoker-landscape-fit` в `src/balatroInferno.css` (`transform-origin`, `will-change`).
- [x] Проверено: `npm run build` проходит, `npm test` проходит.

## Что дальше
- Дождаться валидации на устройстве. Если всё ещё “хаос” — собрать скрин + модель iPhone и точечно править (скорее всего, размеры карт/баннеров).
