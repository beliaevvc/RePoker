# Рефлексия: Отключение Normal режима и чистка шапки

## Реализация vs План
- План выполнен полностью. Normal-режим отключён (как через UI, так и через логику контроллера), но оставлена возможность быстрого возврата через флаг.
- Шапка UI значительно упрощена: убрана лишняя строка `MODE: ...`, кнопки `PAYTABLE` и `/dev` перенесены вниз под плашки статистики.
- Дополнительно (по фидбэку в процессе): добавлена стилизация кнопки Paytable (крупный эмодзи 💵, выравнивание текста).

## Что получилось хорошо
- **Feature Flags**: использование констант `NORMAL_MODE_ENABLED` и `MODE_SWITCHER_UI_ENABLED` позволяет легко управлять доступностью старого режима без удаления кода.
- **UI Layout**: перенос кнопок `PAYTABLE` и `/dev` во второй ряд (под CHIPS/ANTE) освободил верхнюю панель и сделал интерфейс чище.
- **Гибкость**: в процессе итеративно доработали стиль кнопки Paytable (размер иконки, выравнивание текста), не ломая основной функционал.

## Сложности
- Выравнивание эмодзи и текста в кнопке Paytable потребовало нескольких итераций (эмодзи имеют специфические метрики шрифта, которые плохо дружат с flex-center на мелких кеглях). Решилось ручным `mt-[4px]` для текста.

## Что улучшить
- В будущем стоит унифицировать использование иконок: сейчас есть SVG-компоненты (`PixelIcons`) и нативные эмодзи. Лучше придерживаться одного стиля (либо всё SVG, либо всё эмодзи), но пока смешанный подход работает нормально для прототипа.

