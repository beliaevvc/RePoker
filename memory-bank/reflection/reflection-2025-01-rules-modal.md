# Reflection — 2025-01 — Rules Modal

## Контекст / задача
Создать отдельное модальное окно с правилами игры (Rules Modal). Правила должны быть на английском языке, но структура и текст должны быть утверждены на русском перед реализацией. Процесс работы: структура → текст → визуал.

## План vs факт

### План (из `memory-bank/tasks.md` и `creative-rules-modal.md`)

**Этап 1: Creative-фаза (утверждение структуры и текста)**
- ✅ Определить структуру правил игры (разделы, порядок, группировка)
- ✅ Утвердить текст правил на русском языке
- ✅ Подготовить английский перевод текста правил

**Этап 2: Реализация UI**
- ✅ Добавить состояние `rulesModalOpen` в `useBalatroInfernoController.js`
- ✅ Добавить функцию `setRulesModalOpen` в возвращаемый объект контроллера
- ✅ Добавить кнопку RULES в `HeaderBar2` компонент (рядом с PAYTABLE)
- ✅ Создать компонент `RulesModal.jsx` по аналогии с `PaytableModal.jsx`
- ✅ Добавить `RulesModal` в `ModalsHost` компонент
- ✅ Реализовать обработчики открытия/закрытия модального окна
- ✅ Добавить поддержку закрытия по Escape и клику на backdrop

**Дополнительно (не было в первоначальном плане):**
- ✅ Добавлены визуальные элементы для каждого раздела:
  - Кнопки +/- и PLAY в разделе "How to Play"
  - Выигрышная комбинация карт (Three of a Kind) с эффектами свечения
  - CHIPS и ANTE дисплеи в разделе "Betting & Balance"
  - Визуализация каскада с картами (Pair) в разделе "CASCADE Mode"
  - Баннер "TOTAL WIN" с информацией о каскадах и множителях
  - Бейджи множителей (1×, 2×, 3×, 5×) с пиксельной сеткой
  - Миниатюра AutoPlay модального окна в разделе "Auto Play"
  - Кнопка TURBO в разделе "TURBO Mode"
- ❌ Попытка добавить логотип POKERFALL над блоком "HOW TO PLAY" не была завершена (пользователь отклонил изменения)

### Фактическая реализация

**Creative-фаза:**
- Структура правил утверждена: логический порядок от общего к частному
- Порядок разделов: How to Play → Betting & Balance → CASCADE Mode → Hand Combinations → Cascade Multipliers → Jokers → Deck Clear → Auto Play → TURBO Mode
- Все 9 разделов сформулированы на русском и переведены на английский
- Стиль текста: сбалансированный (средняя длина, основные факты + краткие пояснения)
- Визуальное оформление: гибридный подход (обычные разделы — простой стиль, Deck Clear и Jokers — выделение)

**Реализация UI:**
- Компонент `RulesModal.jsx` создан (403 строки)
- Структура аналогична `PaytableModal.jsx`: Header (RULES + Close button) + Scrollable Content + Footer отсутствует
- Все 9 разделов реализованы с английским текстом
- Кнопка RULES добавлена в `HeaderBar2` с эмодзи 📖
- Интеграция в `ModalsHost` выполнена
- Обработчики открытия/закрытия реализованы
- Поддержка Escape и backdrop клика реализована

**Визуальные элементы (добавлены по запросам пользователя):**
- **How to Play:**
  - Кнопки +/- (пропорционально уменьшенные, неинтерактивные)
  - Кнопка PLAY (пропорционально уменьшенная, без эффектов нажатия, выровнена по левому краю)
  - Выигрышная комбинация "Three of a Kind" (A♥ A♠ A♦ 9♣ 5♥) с эффектами свечения (зелёное свечение, электрическая обводка)
- **Betting & Balance:**
  - Убрана информация о стартовом балансе
  - Добавлены CHIPS ($99.20) и ANTE ($1.00) дисплеи (пропорционально уменьшенные, размещены рядом)
- **CASCADE Mode:**
  - Визуализация каскада изменена с "Straight" на "Pair" (A♥ A♠ K♦ 9♣ 5♥ → 10♣ 8♦ K♦ 9♣ 5♥)
  - Текст перемещён под визуализацию карт
  - Убрана чёрная рамка с белыми полями вокруг карт
  - Убран блок "WIN 0.20 WINS (1)"
  - Добавлен баннер "TOTAL WIN" (+$500.00, CASCADES x7, MAX MULT x5) под текстом (пропорционально уменьшен в 2 раза, уменьшен горизонтальный padding, слегка наклонён)
- **Hand Combinations:**
  - Визуализация изменена на "Full House" (A♥ A♠ A♦ K♣ K♥)
  - Текст разбит на два абзаца
  - "PAYTABLE" выделен синим цветом
- **Cascade Multipliers:**
  - Бейджи множителей (1×, 2×, 3×, 5×) стилизованы как `CascadeMultiplierIndicator` с пиксельной сеткой (`pixel-grid-overlay`)
- **Auto Play:**
  - Убрано упоминание "infinite" из текста
  - Убраны кнопки "10" и "25"
  - Добавлен полноценный AutoPlay модальный компонент (пропорционально уменьшенный, включая все кнопки: 10, 25, 50, 100, 500, 1000, START, подсказку) под текстом, слегка наклонённый
- **TURBO Mode:**
  - Текст "TURBO" заменён на пропорционально уменьшенную неинтерактивную кнопку TURBO (аналогичную кнопке на главном экране)
- **Центрирование:**
  - Все заголовки разделов отцентрованы
  - Все параграфы текста отцентрованы
  - Кнопки и элементы выровнены по центру

**Попытка добавить логотип (не завершена):**
- Пользователь запросил добавить логотип POKERFALL над блоком "HOW TO PLAY"
- Была попытка добавить компонент `ResimpleLogo` целиком со всеми эффектами
- Проблема: логотип не имел пиксельной сетки на буквах
- Попытки исправить:
  1. Добавление класса `pixel-grid-overlay` к тексту — не сработало (overlay не работает с `background-clip: text`)
  2. Создание отдельного слоя с пиксельной сеткой через `background-clip: text` — не сработало должным образом
- Пользователь отклонил изменения, логотип не был добавлен

## Что было сложным / почему

### 1. Визуальная консистентность с существующими компонентами
**Проблема:** Пользователь требовал, чтобы все визуальные элементы в Rules Modal были "один в один" с элементами на главном экране (кнопки, карты, баннеры, дисплеи).

**Сложность:**
- Нужно было изучать существующие компоненты (`HeaderBar2`, `FooterControls2`, `CascadeMultiplierIndicator`, `AutoPlayModal`, `MiniCard`, `Card`) и точно копировать их стили
- Многие элементы требовали пропорционального уменьшения, что усложняло точное копирование
- Некоторые элементы (например, баннер "TOTAL WIN") имели сложную структуру с несколькими слоями и анимациями

**Решение:**
- Тщательное изучение исходного кода существующих компонентов
- Копирование классов Tailwind и inline стилей
- Использование пропорциональных коэффициентов для уменьшения размеров (например, уменьшение padding, border-width, font-size в 2 раза)

### 2. Эффекты выигрышных карт
**Проблема:** Нужно было добавить эффекты свечения и обводки для выигрышных карт в Rules Modal.

**Сложность:**
- Компонент `MiniCard` изначально не имел поддержки эффектов выигрышных карт
- Нужно было добавить `ElectricBorder` (SVG с анимированной пунктирной обводкой) и `PixelFire` (градиентное свечение с пульсацией)
- Эффекты должны были быть видимыми, но не слишком сильными

**Решение:**
- Добавлены компоненты `ElectricBorder` и `PixelFire` в `MiniCard.jsx`
- Настроены `z-index` и `overflow: visible` для правильного отображения эффектов
- Итеративная настройка интенсивности эффектов (blur, opacity, strokeWidth) по запросам пользователя

### 3. Пиксельная сетка на логотипе
**Проблема:** Пользователь хотел добавить логотип POKERFALL с пиксельной сеткой на буквах, но стандартный подход с `pixel-grid-overlay` не работал.

**Сложность:**
- Класс `pixel-grid-overlay` использует `::after` псевдоэлемент, который создаёт overlay поверх элемента
- Текст логотипа использует `background-clip: text` и `color: transparent`, что делает псевдоэлемент невидимым или некорректно отображаемым
- Попытка создать отдельный слой с пиксельной сеткой через `background-clip: text` также не дала желаемого результата

**Решение:**
- Проблема не была решена полностью — пользователь отклонил изменения
- Возможные решения для будущего:
  - Использовать SVG маску для пиксельной сетки
  - Применить пиксельную сетку через комбинацию градиентов в самом `logo-gradient-gold`
  - Использовать CSS `mask-image` для обрезки сетки по форме текста

### 4. Множественные итерации по визуальным элементам
**Проблема:** Пользователь запрашивал множество изменений визуальных элементов в процессе реализации.

**Сложность:**
- Каждое изменение требовало изучения соответствующего компонента на главном экране
- Некоторые изменения были противоречивыми (например, сначала добавить элемент, потом убрать, потом добавить снова с другими параметрами)
- Много мелких правок (центрирование, выравнивание, размеры, отступы)

**Решение:**
- Терпеливое выполнение всех запросов пользователя
- Тщательное чтение DOM Path, который пользователь предоставлял для идентификации элементов
- Использование `search_replace` для точных изменений

## Какие решения оказались удачными

### 1. Структура правил (Creative-фаза)
**Решение:** Логический порядок разделов от общего к частному.

**Почему удачно:**
- Новый игрок сразу понимает базовый геймплей
- Механики идут от простых к сложным
- Структура интуитивно понятна

### 2. Гибридный подход к визуальному оформлению
**Решение:** Обычные разделы — простой стиль, важные разделы (Deck Clear, Jokers) — выделение.

**Почему удачно:**
- Визуальная иерархия помогает выделить важную информацию
- Не перегружает интерфейс
- Соответствует стилю `PaytableModal`

### 3. Использование существующих компонентов для визуальных элементов
**Решение:** Копирование стилей и структуры из существующих компонентов (`HeaderBar2`, `FooterControls2`, `CascadeMultiplierIndicator`, `AutoPlayModal`).

**Почему удачно:**
- Обеспечивает визуальную консистентность
- Пользователь сразу узнаёт элементы из игры
- Упрощает понимание правил через визуальные примеры

### 4. Текстовое упоминание Paytable вместо интерактивной ссылки
**Решение:** Текст "See the PAYTABLE button in the header for the full list of combinations and payouts."

**Почему удачно:**
- Просто реализовать
- Не перегружает UI
- Понятно для игрока
- Избегает сложности управления двумя модальными окнами одновременно

## Какие решения оказались неудачными

### 1. Попытка добавить логотип с пиксельной сеткой
**Решение:** Добавление компонента `ResimpleLogo` с попыткой применить пиксельную сетку через стандартные CSS классы.

**Почему неудачно:**
- Стандартный подход `pixel-grid-overlay` не работает с `background-clip: text`
- Попытка создать отдельный слой с сеткой также не дала желаемого результата
- Пользователь отклонил изменения

**Что можно улучшить:**
- Изучить, как пиксельная сетка применяется к тексту в других местах проекта
- Использовать более продвинутые CSS техники (SVG маски, комбинация градиентов)
- Возможно, пиксельная сетка должна быть частью самого градиента текста, а не отдельным слоем

### 2. Первоначальный подход к визуальным элементам
**Решение:** Изначально были добавлены простые иконки и эмодзи для визуализации разделов.

**Почему неудачно:**
- Пользователь сразу указал, что визуальные элементы должны быть "один в один" с элементами игры
- Пришлось переделывать все визуальные элементы

**Что можно улучшить:**
- Сразу уточнять у пользователя требования к визуальным элементам
- Изучать существующие компоненты перед добавлением визуальных элементов
- Предлагать использовать существующие компоненты вместо создания новых

## Что улучшить в процессе

### 1. Уточнение требований к визуальным элементам
**Проблема:** Много итераций по визуальным элементам из-за неясных требований.

**Улучшение:**
- В Creative-фазе сразу уточнять, какие визуальные элементы нужны в каждом разделе
- Предлагать использовать существующие компоненты вместо создания новых
- Спрашивать о требованиях к пропорциям и размерам элементов

### 2. Изучение существующих компонентов перед реализацией
**Проблема:** Некоторые визуальные элементы пришлось переделывать, потому что они не соответствовали существующим компонентам.

**Улучшение:**
- Перед добавлением визуального элемента изучать соответствующий компонент на главном экране
- Копировать структуру и стили напрямую из существующих компонентов
- Использовать инструменты браузера (DOM Path) для точной идентификации элементов

### 3. Подход к пиксельной сетке на тексте
**Проблема:** Стандартный подход `pixel-grid-overlay` не работает с текстом, использующим `background-clip: text`.

**Улучшение:**
- Изучить, как пиксельная сетка применяется к тексту в других местах проекта
- Исследовать альтернативные подходы (SVG маски, комбинация градиентов, CSS `mask-image`)
- Возможно, создать специальный CSS класс для пиксельной сетки на тексте с градиентом

### 4. Документирование визуальных решений
**Проблема:** Много визуальных элементов было добавлено по запросам пользователя, но не все решения были задокументированы в Creative-фазе.

**Улучшение:**
- В Creative-фазе документировать все визуальные элементы, которые будут использоваться
- Указывать источники визуальных элементов (из каких компонентов они берутся)
- Документировать пропорции и размеры элементов

## Технические детали реализации

### Файлы, которые были изменены:
1. `src/ui/screens/balatro-inferno/components/RulesModal.jsx` (новый файл, 403 строки)
2. `src/ui/screens/balatro-inferno/BalatroInferno.jsx` (добавлена кнопка RULES в HeaderBar2)
3. `src/ui/screens/balatro-inferno/useBalatroInfernoController.js` (добавлено состояние `rulesModalOpen`)
4. `src/ui/screens/balatro-inferno/components/MiniCard.jsx` (добавлены эффекты выигрышных карт: `ElectricBorder`, `PixelFire`)

### Архитектурные решения:
- Компонент `RulesModal` следует той же структуре, что и `PaytableModal`
- Используется существующий паттерн модальных окон через `ModalsHost`
- Состояние модального окна управляется через контроллер (`useBalatroInfernoController`)
- Визуальные элементы переиспользуют стили из существующих компонентов

### Производительность:
- Модальное окно рендерится только когда открыто (`if (!open) return null`)
- Используется `React.memo` для оптимизации (через `ModalsHost`)
- Визуальные элементы (кнопки, карты) неинтерактивные, что снижает нагрузку

## Итоги

**Что получилось хорошо:**
- Структура правил логична и понятна
- Визуальные элементы консистентны с игрой
- Модальное окно работает стабильно
- Код следует архитектуре проекта

**Что можно улучшить:**
- Добавить логотип с пиксельной сеткой (требует дополнительного исследования)
- Улучшить процесс уточнения требований к визуальным элементам
- Документировать визуальные решения в Creative-фазе

**Общая оценка:**
Задача выполнена успешно. Модальное окно с правилами создано, все разделы реализованы, визуальные элементы добавлены и соответствуют стилю игры. Единственная незавершённая часть — добавление логотипа с пиксельной сеткой, но это не критично для функциональности модального окна.

