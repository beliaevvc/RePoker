# Архив — Rules Modal (Модальное окно с правилами игры)

**Дата:** 2025-01-25  
**Ветка:** `guide` → `main` (слита)  
**Статус:** ✅ Завершено и слито в main

---

## Summary

Создано отдельное модальное окно с правилами игры (Rules Modal). Правила структурированы в 9 разделов, отображаются на английском языке, структура и тексты были утверждены на русском в Creative-фазе. Модальное окно включает визуальные элементы из игры (кнопки, карты, баннеры, дисплеи) для лучшего понимания правил. Кнопка RULES добавлена в HeaderBar2 рядом с PAYTABLE. Модальное окно работает аналогично PaytableModal с поддержкой закрытия по Escape и клику на backdrop.

---

## Requirements

### Исходные требования
- Добавить кнопку "RULES" в HeaderBar2 (похожую на PAYTABLE)
- Создать модальное окно RulesModal (похожее на PaytableModal)
- Правила должны быть на английском языке
- Структуру и текст нужно утвердить на русском перед реализацией
- Этапы работы: структура → текст → визуал

### Дополнительные требования (в процессе реализации)
- Добавить визуальные элементы для каждого раздела (кнопки, карты, баннеры, дисплеи)
- Визуальные элементы должны быть "один в один" с элементами на главном экране
- Добавить эффекты выигрышных карт (свечение, обводка) в компонент MiniCard
- Все заголовки и тексты должны быть отцентрованы

---

## Implementation

### 1. Creative-фаза (утверждение структуры и текста)

**Файл:** `memory-bank/creative/creative-rules-modal.md`

**Решения:**
- **Структура:** Логический порядок от общего к частному
  - Порядок разделов: How to Play → Betting & Balance → CASCADE Mode → Hand Combinations → Cascade Multipliers → Jokers → Deck Clear → Auto Play → TURBO Mode
- **Стиль текста:** Сбалансированный (средняя длина, основные факты + краткие пояснения)
- **Визуальное оформление:** Гибридный подход (обычные разделы — простой стиль, Deck Clear и Jokers — выделение)
- **Ссылка на Paytable:** Текстовое упоминание с подсказкой о расположении кнопки PAYTABLE

**Результат:**
- Все 9 разделов сформулированы на русском и переведены на английский
- Структура утверждена пользователем
- Тексты утверждены пользователем

### 2. Состояние модального окна

**Файл:** `src/ui/screens/balatro-inferno/useBalatroInfernoController.js`

**Изменения:**
- Добавлено состояние `const [rulesModalOpen, setRulesModalOpen] = useState(false)`
- Состояние включено в возвращаемый объект контроллера

### 3. Кнопка RULES в HeaderBar2

**Файл:** `src/ui/screens/balatro-inferno/BalatroInferno.jsx`

**Изменения:**
- Добавлена кнопка RULES рядом с PAYTABLE в компоненте HeaderBar2
- Использована аналогичная структура и стили как у PAYTABLE
- Добавлен эмодзи 📖 для визуального отличия
- Реализован обработчик `openRules` через `useCallback`

**Код:**
```jsx
<button
  type="button"
  onClick={openRules}
  className={[
    'inline-flex items-center gap-2 h-7',
    'text-[10px] font-bold text-slate-400 hover:text-white uppercase tracking-widest',
    'border-b border-transparent hover:border-slate-400 transition-all group',
  ].join(' ')}
>
  <span className="text-xl leading-none filter grayscale-[0.3] group-hover:grayscale-0 group-hover:scale-110 transition-all duration-200" aria-hidden="true" role="img">📖</span>
  <span className="leading-none mt-[4px]">RULES</span>
</button>
```

### 4. Компонент RulesModal

**Файл:** `src/ui/screens/balatro-inferno/components/RulesModal.jsx` (новый файл, 403 строки)

**Структура:**
- Header: "RULES" + Close button (X)
- Scrollable Content: 9 разделов правил
- Footer: отсутствует

**Разделы:**
1. **How to Play**
   - Кнопки +/- (пропорционально уменьшенные, неинтерактивные)
   - Кнопка PLAY (пропорционально уменьшенная, без эффектов нажатия)
   - Выигрышная комбинация "Three of a Kind" (A♥ A♠ A♦ 9♣ 5♥) с эффектами свечения

2. **Betting & Balance**
   - CHIPS ($99.20) и ANTE ($1.00) дисплеи (пропорционально уменьшенные, размещены рядом)
   - Убрана информация о стартовом балансе

3. **CASCADE Mode**
   - Визуализация каскада с картами "Pair" (A♥ A♠ K♦ 9♣ 5♥ → 10♣ 8♦ K♦ 9♣ 5♥)
   - Баннер "TOTAL WIN" (+$500.00, CASCADES x7, MAX MULT x5) под текстом (пропорционально уменьшен в 2 раза)

4. **Hand Combinations**
   - Визуализация "Full House" (A♥ A♠ A♦ K♣ K♥)
   - Текст разбит на два абзаца
   - "PAYTABLE" выделен синим цветом

5. **Cascade Multipliers**
   - Бейджи множителей (1×, 2×, 3×, 5×) стилизованы как `CascadeMultiplierIndicator` с пиксельной сеткой (`pixel-grid-overlay`)

6. **Jokers**
   - Две миниатюры джокеров с эффектами свечения
   - Фиолетовая цветовая схема (аналогично PaytableModal)

7. **Deck Clear**
   - Выделен визуально (amber border + градиент, аналогично PaytableModal)

8. **Auto Play**
   - Миниатюра AutoPlay модального окна (пропорционально уменьшенная, включая все кнопки: 10, 25, 50, 100, 500, 1000, START, подсказку)
   - Убрано упоминание "infinite"

9. **TURBO Mode**
   - Кнопка TURBO (пропорционально уменьшенная, неинтерактивная, аналогичная кнопке на главном экране)

**Особенности реализации:**
- Все заголовки разделов отцентрованы
- Все параграфы текста отцентрованы
- Визуальные элементы переиспользуют стили из существующих компонентов (`HeaderBar2`, `FooterControls2`, `CascadeMultiplierIndicator`, `AutoPlayModal`)
- Используется компонент `MiniCard` для отображения карт
- Применены эффекты выигрышных карт (зелёное свечение, электрическая обводка)

### 5. Эффекты выигрышных карт в MiniCard

**Файл:** `src/ui/screens/balatro-inferno/components/MiniCard.jsx`

**Изменения:**
- Добавлен компонент `ElectricBorder` (SVG с анимированной пунктирной обводкой)
- Добавлен компонент `PixelFire` (градиентное свечение с пульсацией)
- Настроены `z-index` и `overflow: visible` для правильного отображения эффектов
- Интенсивность эффектов настроена итеративно по запросам пользователя

### 6. Интеграция в ModalsHost

**Файл:** `src/ui/screens/balatro-inferno/BalatroInferno.jsx`

**Изменения:**
- Добавлен `RulesModal` в компонент `ModalsHost`
- Переданы props `rulesModalOpen` и `setRulesModalOpen` из контроллера
- Реализованы обработчики `openRules` и `closeRulesModal` через `useCallback`

**Обработчики:**
- Открытие: клик на кнопку RULES
- Закрытие: Escape, клик на backdrop, клик на кнопку X

---

## Testing

### Технические проверки
- ✅ Линтер: ошибок нет (`npm run lint`)
- ✅ Сборка: проект собирается без ошибок (`npm run build`)
- ✅ TypeScript: типы корректны

### Функциональные проверки
- ✅ Модальное окно открывается по клику на кнопку RULES
- ✅ Модальное окно закрывается по Escape
- ✅ Модальное окно закрывается по клику на backdrop
- ✅ Модальное окно закрывается по клику на кнопку X
- ✅ Все 9 разделов правил отображаются корректно
- ✅ Визуальные элементы отображаются корректно
- ✅ Прокрутка работает корректно (custom-scrollbar)

### Визуальные проверки
- ✅ Кнопка RULES визуально соответствует PAYTABLE
- ✅ Модальное окно соответствует стилю PaytableModal
- ✅ Визуальные элементы соответствуют элементам на главном экране
- ✅ Эффекты выигрышных карт работают корректно
- ✅ Все элементы отцентрованы корректно

---

## Lessons Learned

### Удачные решения

1. **Логическая структура правил**
   - Порядок разделов от общего к частному помогает новым игрокам быстро понять базовый геймплей
   - Механики идут от простых к сложным

2. **Гибридный подход к визуальному оформлению**
   - Обычные разделы имеют простой стиль, важные разделы (Deck Clear, Jokers) выделены
   - Создаёт визуальную иерархию без перегрузки интерфейса

3. **Использование существующих компонентов для визуальных элементов**
   - Копирование стилей и структуры из существующих компонентов обеспечивает визуальную консистентность
   - Пользователь сразу узнаёт элементы из игры
   - Упрощает понимание правил через визуальные примеры

4. **Текстовое упоминание Paytable вместо интерактивной ссылки**
   - Просто реализовать
   - Не перегружает UI
   - Понятно для игрока
   - Избегает сложности управления двумя модальными окнами одновременно

5. **Добавление эффектов выигрышных карт в MiniCard**
   - Эффекты переиспользуются в других местах проекта
   - Визуально привлекательно и помогает понять концепцию выигрышных комбинаций

### Что было сложным

1. **Визуальная консистентность с существующими компонентами**
   - Нужно было изучать существующие компоненты и точно копировать их стили
   - Многие элементы требовали пропорционального уменьшения, что усложняло точное копирование
   - Некоторые элементы (например, баннер "TOTAL WIN") имели сложную структуру с несколькими слоями и анимациями

2. **Эффекты выигрышных карт**
   - Компонент `MiniCard` изначально не имел поддержки эффектов выигрышных карт
   - Нужно было добавить `ElectricBorder` и `PixelFire` компоненты
   - Эффекты должны были быть видимыми, но не слишком сильными (требовалась итеративная настройка)

3. **Множественные итерации по визуальным элементам**
   - Пользователь запрашивал множество изменений визуальных элементов в процессе реализации
   - Каждое изменение требовало изучения соответствующего компонента на главном экране
   - Некоторые изменения были противоречивыми (например, сначала добавить элемент, потом убрать, потом добавить снова с другими параметрами)

4. **Пиксельная сетка на логотипе (не завершено)**
   - Попытка добавить логотип POKERFALL с пиксельной сеткой на буквах не была завершена
   - Стандартный подход `pixel-grid-overlay` не работает с текстом, использующим `background-clip: text`
   - Попытка создать отдельный слой с пиксельной сеткой через `background-clip: text` также не дала желаемого результата
   - Пользователь отклонил изменения

### Что улучшить в процессе

1. **Уточнение требований к визуальным элементам**
   - В Creative-фазе сразу уточнять, какие визуальные элементы нужны в каждом разделе
   - Предлагать использовать существующие компоненты вместо создания новых
   - Спрашивать о требованиях к пропорциям и размерам элементов

2. **Изучение существующих компонентов перед реализацией**
   - Перед добавлением визуального элемента изучать соответствующий компонент на главном экране
   - Копировать структуру и стили напрямую из существующих компонентов
   - Использовать инструменты браузера (DOM Path) для точной идентификации элементов

3. **Подход к пиксельной сетке на тексте**
   - Изучить, как пиксельная сетка применяется к тексту в других местах проекта
   - Исследовать альтернативные подходы (SVG маски, комбинация градиентов, CSS `mask-image`)
   - Возможно, создать специальный CSS класс для пиксельной сетки на тексте с градиентом

4. **Документирование визуальных решений**
   - В Creative-фазе документировать все визуальные элементы, которые будут использоваться
   - Указывать источники визуальных элементов (из каких компонентов они берутся)
   - Документировать пропорции и размеры элементов

---

## References

- **Reflection:** `memory-bank/reflection/reflection-2025-01-rules-modal.md`
- **Creative:** `memory-bank/creative/creative-rules-modal.md`
- **Tasks:** `memory-bank/tasks.md` (задача завершена)
- **Файлы изменений:**
  - `src/ui/screens/balatro-inferno/components/RulesModal.jsx` — новый компонент (403 строки)
  - `src/ui/screens/balatro-inferno/BalatroInferno.jsx` — добавлена кнопка RULES в HeaderBar2, интеграция в ModalsHost
  - `src/ui/screens/balatro-inferno/useBalatroInfernoController.js` — добавлено состояние `rulesModalOpen`
  - `src/ui/screens/balatro-inferno/components/MiniCard.jsx` — добавлены эффекты выигрышных карт (`ElectricBorder`, `PixelFire`)

---

## Выводы

Задача выполнена успешно. Модальное окно с правилами создано, все разделы реализованы, визуальные элементы добавлены и соответствуют стилю игры. Единственная незавершённая часть — добавление логотипа с пиксельной сеткой, но это не критично для функциональности модального окна.

**Уровень сложности:** Level 2 (простое улучшение) — оценка была корректной, но задача оказалась более объёмной из-за множества визуальных элементов.

**Время реализации:** ~2-3 часа (Creative-фаза + реализация UI + множественные итерации по визуальным элементам)

**Ключевые достижения:**
- Структура правил логична и понятна
- Визуальные элементы консистентны с игрой
- Модальное окно работает стабильно
- Код следует архитектуре проекта

