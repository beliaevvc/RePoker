# Архив задачи: Отключение Normal режима, CASCADE по умолчанию

**Дата:** 23.12.2025
**Статус:** Завершено

## Summary
В этой задаче был отключён режим "Normal" (стандартный покер) в пользу режима "Cascade" как основного и единственного доступного пользователю. UI был упрощён (убраны переключатели режимов, надпись MODE), а кнопки `PAYTABLE` и `/dev` перенесены в более удобное место под основные плашки статистики. Реализована возможность быстрого возврата функционала через feature-flags в коде.

## Requirements
1.  **Отключить Normal режим**: по умолчанию всегда CASCADE, игнорировать старые сохранения `normal` в localStorage.
2.  **Очистить UI**: убрать надпись `MODE: CASCADE` и кнопки переключения режимов.
3.  **Перекомпоновка**: перенести кнопку `PAYTABLE` в левый угол (под CHIPS), кнопку `DEV` (теперь `/dev`) в правый (под ANTE).
4.  **Стилизация**: добавить стильную иконку (эмодзи 💵) к кнопке Paytable.
5.  **Сохранить логику**: не удалять код Normal режима, а скрыть его за флагом, чтобы можно было включить обратно.

## Implementation

### 1. Feature Flags
В `useBalatroInfernoController.js` и `BalatroInferno.jsx` введены константы:
-   `NORMAL_MODE_ENABLED = false`: Контроллер принудительно устанавливает режим `cascade` при инициализации, даже если в localStorage записан `normal`.
-   `MODE_SWITCHER_UI_ENABLED = false`: Скрывает блок с переключателем режимов и надписью `MODE: ...`.

### 2. UI Changes (`BalatroInferno.jsx`)
-   Убран верхний ряд с `MODE: ...` и переключателем.
-   Создан новый ряд кнопок **под** плашками `CHIPS` и `ANTE`:
    -   Слева: Кнопка **PAYTABLE** с крупным эмодзи 💵 (`text-xl`) и выровненным текстом.
    -   Справа: Текстовая кнопка **/dev** (открывает DevTools).
-   Кнопка Paytable стилизована: эмодзи имеет эффект `grayscale` + `scale` при наведении, текст выровнен вручную (`mt-[4px]`) для визуальной симметрии.

### 3. Controller Logic (`useBalatroInfernoController.js`)
-   Функция `readStoredGameMode` модифицирована для проверки флага `NORMAL_MODE_ENABLED`.

## Testing
-   **Manual**:
    -   При загрузке всегда включается режим CASCADE.
    -   В `localStorage` значение перезаписывается на `cascade`.
    -   Кнопка Paytable открывает модалку, `/dev` открывает панель разработчика.
    -   Лейаут на мобильных и десктопах корректен (кнопки под плашками).
-   **Automated**:
    -   `npm run lint`: пройден.
    -   `npm test`: 24/24 тестов пройдены (изменения не затронули игровую логику, только инициализацию и UI).
    -   `npm run build`: сборка успешна.

## Lessons Learned
-   Выравнивание эмодзи с текстом (особенно мелким CAPS) — нетривиальная задача из-за метрик шрифта эмодзи. Проще использовать ручной `margin-top` или `translate-y`, чем пытаться добиться идеала через `flex-align`.
-   Использование Feature Flags для "отключения" функционала — отличная практика, позволяющая быстро откатить изменения или вернуть режим для тестов без git revert.

## References
-   Reflection: `memory-bank/reflection/reflection-2025-12-23-disable-normal-mode.md`

