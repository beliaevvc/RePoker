/**
 * Файл: src/domain/ports/rng.ts
 * Слой: domain (port)
 * Назначение: абстракция генератора случайных чисел для доменных операций (например, создание/перемешивание колоды).
 *
 * Инварианты:
 * - В domain не должно быть прямых обращений к `Math.random()` после Этапа 5.
 */

/**
 * Порт RNG: минимальный контракт — случайное число в диапазоне [0, 1).
 */
export type Rng = {
  /**
   * @returns число в диапазоне [0, 1)
   */
  randomFloat(): number
}


